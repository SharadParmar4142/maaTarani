# Maa Tarani Frontend (React + Vite)

This folder is the active React frontend.

## Run Locally

1. Install dependencies:

```bash
cd Frontend/react-app
npm install
```

1. Start development server:

```bash
npm run dev
```

1. Create production build:

```bash
npm run build
```
